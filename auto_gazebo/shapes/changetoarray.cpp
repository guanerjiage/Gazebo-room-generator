
#include <iostream>
#include "bitmap_image.hpp"
using namespace std;

int main()
{
    cout<<"hello";
    /*
    std::string file_name("shape0.png");
    bitmap_image image(file_name);
    unsigned int w = image.width();
    unsigned int h = image.height();
    for(int i=0; i<h;i++)
    {
        for(int j=0;j<w;j++)
        {
            unsigned char r,g,b;
            image.get_pixel(i,j,r,g,b);
            std::cout<<r<<' '<<g<<' '<<b<<' ';
        }
        std::cout<<std::endl;
    }
    */
   return 0;
}